# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=securescan
DB_PASSWORD=securescan
DB_NAME=securescan
DB_SSLMODE=disable

# Server
API_PORT=3000
FRONTEND_URL=http://localhost:5173

# Scan workspace (where repos get cloned)
SCAN_WORKSPACE=/tmp/securescan

# GitHub (for PR creation)
GITHUB_TOKEN=

# Claude API (for AI fixes)
ANTHROPIC_API_KEY=
